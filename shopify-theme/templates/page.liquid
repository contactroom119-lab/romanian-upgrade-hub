{%- comment -%}
  Minimal page stub (renders page.content, e.g. a Contact page for the custom
  order CTA). Not part of the design scope. See README.
{%- endcomment -%}
<section class="r119-section r119-container">
  <div class="r119-section__head">
    <div><h2 class="r119-h2">{{ page.title }}</h2></div>
  </div>
  <div class="rte" style="max-width: 720px;">{{ page.content }}</div>
</section>
